/** Get a guild scheduled event. */
export async function getScheduledEvent(bot, guildId, eventId, options) {
    const result = await bot.rest.runMethod(bot.rest, "GET", bot.constants.routes.GUILD_SCHEDULED_EVENT(guildId, eventId, options?.withUserCount));
    return bot.transformers.scheduledEvent(bot, result);
}

/** Kick a member from the server */
export async function kickMember(bot, guildId, memberId, reason) {
    return await bot.rest.runMethod(bot.rest, "DELETE", bot.constants.routes.GUILD_MEMBER(guildId, memberId), {
        reason,
    });
}

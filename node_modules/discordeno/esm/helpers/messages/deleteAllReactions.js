/** Deletes all reactions for all emojis on this message. */
export async function deleteAllReactions(bot, channelId, messageId) {
    return await bot.rest.runMethod(bot.rest, "DELETE", bot.constants.routes.CHANNEL_MESSAGE_REACTIONS(channelId, messageId));
}

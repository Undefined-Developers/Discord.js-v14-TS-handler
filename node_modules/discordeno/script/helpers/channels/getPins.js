"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPins = void 0;
const collection_js_1 = require("../../util/collection.js");
/** Get pinned messages in this channel. */
async function getPins(bot, channelId) {
    const results = await bot.rest.runMethod(bot.rest, "GET", bot.constants.routes.CHANNEL_PINS(channelId));
    return new collection_js_1.Collection(results.map((result) => {
        const message = bot.transformers.message(bot, result);
        return [message.id, message];
    }));
}
exports.getPins = getPins;

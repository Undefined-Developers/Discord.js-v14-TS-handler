"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const permissions_js_1 = require("../permissions.js");
function createWebhook(bot) {
    const createWebhookOld = bot.helpers.createWebhook;
    bot.helpers.createWebhook = async function (channelId, options) {
        (0, permissions_js_1.requireBotChannelPermissions)(bot, channelId, ["MANAGE_WEBHOOKS", "VIEW_CHANNEL"]);
        if (
        // Specific usernames that discord does not allow
        ["clyde", "everyone", "here"].includes(options.name) ||
            options.name.includes("discord") ||
            options.name.includes("@") ||
            options.name.includes("#") ||
            options.name.includes(":") ||
            options.name.includes("\/") ||
            !bot.utils.validateLength(options.name, { min: 2, max: 32 })) {
            throw new Error("The webhook name can not be clyde, everyone, here or include 'discord, @, #, :, ' and it must be between 2 and 32 characters long.");
        }
        return await createWebhookOld(channelId, options);
    };
}
exports.default = createWebhook;

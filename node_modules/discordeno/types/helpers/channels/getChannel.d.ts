import type { Bot } from "../../bot.js";
import { Channel } from "../../transformers/channel.js";
/** Fetches a single channel object from the api. */
export declare function getChannel(bot: Bot, channelId: bigint): Promise<Channel>;

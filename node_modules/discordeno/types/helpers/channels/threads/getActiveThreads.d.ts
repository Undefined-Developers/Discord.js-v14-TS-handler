import type { Bot } from "../../../bot.js";
import { Channel, ThreadMember } from "../../../mod.js";
import { Collection } from "../../../util/collection.js";
export declare type ActiveThreads = {
    threads: Collection<bigint, Channel>;
    members: Collection<bigint, ThreadMember>;
};
/** Returns all active threads in the guild, including public and private threads. Threads are ordered by their `id`, in descending order. */
export declare function getActiveThreads(bot: Bot, guildId: bigint): Promise<ActiveThreads>;

import type { Bot } from "../../../bot.js";
import { ThreadMember } from "../../../transformers/threadMember.js";
/** Returns thread members objects that are members of the thread. */
export declare function getThreadMember(bot: Bot, threadId: bigint, userId: bigint): Promise<ThreadMember>;

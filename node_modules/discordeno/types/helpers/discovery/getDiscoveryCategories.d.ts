import type { Bot } from "../../bot.js";
import { Collection } from "../../util/collection.js";
export declare type DiscoveryCategory = {
    id: bigint;
    name: DiscoveryName;
    isPrimary: boolean;
};
export declare type DiscoveryName = {
    default: string;
    localizations?: Record<string, string>;
};
/** Returns a Collection (mapped by Id of the discovery category object) of discovery category objects that can be used when editing guilds */
export declare function getDiscoveryCategories(bot: Bot): Promise<Collection<bigint, DiscoveryCategory>>;

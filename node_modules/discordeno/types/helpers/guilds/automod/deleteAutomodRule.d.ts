import { Bot } from "../../../bot.js";
/** Delete a rule currently configured for guild. */
export declare function deleteAutomodRule(bot: Bot, guildId: bigint, ruleId: bigint, reason?: string): Promise<void>;

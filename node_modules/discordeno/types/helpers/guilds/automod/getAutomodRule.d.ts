import { Bot } from "../../../bot.js";
import { AutoModerationRule } from "../../../transformers/automodRule.js";
/** Get a rule currently configured for guild. */
export declare function getAutomodRule(bot: Bot, guildId: bigint, ruleId: bigint): Promise<AutoModerationRule>;

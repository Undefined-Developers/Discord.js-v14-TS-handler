import type { Bot } from "../../bot.js";
import { Emoji } from "../../transformers/emoji.js";
import { Sticker } from "../../transformers/sticker.js";
import { GuildFeatures } from "../../types/shared.js";
export declare type GuildPreview = {
    id: bigint;
    name?: string;
    icon?: string;
    splash?: string;
    discoverySplash?: string;
    emojis?: Emoji[];
    features: GuildFeatures[];
    approximateMemberCount: number;
    approximatePresenceCount: number;
    description?: string;
    stickers: Sticker[];
};
/** Returns the guild preview object for the given id. If the bot is not in the guild, then the guild must be Discoverable. */
export declare function getGuildPreview(bot: Bot, guildId: bigint): Promise<GuildPreview>;

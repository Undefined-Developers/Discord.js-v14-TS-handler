import type { Bot } from "../../bot.js";
import { GuildWidget } from "../../transformers/widget.js";
/** Returns the widget for the guild. */
export declare function getWidget(bot: Bot, guildId: bigint): Promise<GuildWidget>;

import type { Bot } from "../../../bot.js";
import { ApplicationCommandPermission } from "../../../transformers/applicationCommandPermission.js";
/** Fetches command permissions for a specific command for your application in a guild. Returns a GuildApplicationCommandPermissions object. */
export declare function getApplicationCommandPermission(bot: Bot, guildId: bigint, commandId: bigint): Promise<ApplicationCommandPermission>;

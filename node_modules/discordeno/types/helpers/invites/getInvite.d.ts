import type { Bot } from "../../bot.js";
import { ScheduledEvent, TargetTypes, User } from "../../mod.js";
export declare type BaseInvite = {
    code: string;
    guildId?: bigint;
    channelId?: bigint;
    inviter?: User;
    targetType?: TargetTypes;
    targetUser?: User;
    targetApplicationId?: bigint;
    approximatePresenceCount?: number;
    approximateMemberCount?: number;
    expiresAt?: number;
    guildScheduledEvent?: ScheduledEvent;
};
export declare type InviteMetadata = BaseInvite & {
    uses: number;
    maxUses: number;
    maxAge: number;
    temporary: boolean;
    createdAt: number;
};
/** Returns an invite for the given code or throws an error if the invite doesn't exists. */
export declare function getInvite(bot: Bot, inviteCode: string, options?: GetInvite): Promise<BaseInvite>;
/** https://discord.com/developers/docs/resources/invite#get-invite */
export interface GetInvite {
    /** Whether the invite should contain approximate member counts */
    withCounts?: boolean;
    /** Whether the invite should contain the expiration date */
    withExpiration?: boolean;
    /** the guild scheduled event to include with the invite */
    scheduledEventId?: bigint;
}

import type { Bot } from "../../bot.js";
import { Channel } from "../../transformers/channel.js";
/** Get a user's dm channel. This is required in order to send a DM. */
export declare function getDmChannel(bot: Bot, userId: bigint): Promise<Channel>;

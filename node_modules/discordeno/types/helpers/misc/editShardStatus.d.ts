import type { Bot } from "../../bot.js";
import { Activity } from "../../transformers/activity.js";
import { PresenceStatus } from "../../types/shared.js";
export declare function editShardStatus(bot: Bot, shardId: number, data: StatusUpdate): Promise<void>;
/** https://discord.com/developers/docs/topics/gateway#update-status */
export interface StatusUpdate {
    /** The user's activities */
    activities: Activity[];
    /** The user's new status */
    status: keyof typeof PresenceStatus;
}

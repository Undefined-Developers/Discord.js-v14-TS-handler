import type { Bot } from "../../bot.js";
import { Template } from "../../transformers/template.js";
/** Returns the guild template if it exists */
export declare function getGuildTemplate(bot: Bot, templateCode: string): Promise<Template>;

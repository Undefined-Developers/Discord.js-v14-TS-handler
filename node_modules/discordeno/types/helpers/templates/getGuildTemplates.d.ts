import type { Bot } from "../../bot.js";
import { Template } from "../../transformers/template.js";
import { Collection } from "../../util/collection.js";
/** Returns an array of templates. Requires the `MANAGE_GUILD` permission. */
export declare function getGuildTemplates(bot: Bot, guildId: bigint): Promise<Collection<string, Template>>;

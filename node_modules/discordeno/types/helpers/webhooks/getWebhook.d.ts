import type { Bot } from "../../bot.js";
import { Webhook } from "../../transformers/webhook.js";
/** Returns the new webhook object for the given id. */
export declare function getWebhook(bot: Bot, webhookId: bigint): Promise<Webhook>;
